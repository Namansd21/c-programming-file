#include<math.h>
#include <stdio.h>

int main()
{
    int a;// no whoose power to be put
    int b;//power to which a raised
    printf("please enter the base no : ");
    scanf("%d",&a);
    printf("please enter the power of the base no : ");
    scanf("%d",&b);
    printf("your operation result is %f",pow(a,b));
    

    return 0;
}
