#include<math.h>
#include <stdio.h>

int main()
{
    int a ; //no by user to be saved
    
    printf("please enter the no whoose square root you want to find : ");
    scanf("%d",&a);
    printf("square root of given no is %f",sqrt(a));

    return 0;
}
